// Brickford — AI 310 question bank (30). Each sitting draws 12 at random.
// Coverage: the papers canon (12), systems math (9), LLM engineering practice (9).
window.DAR = window.DAR || {};
DAR.QUIZZES = DAR.QUIZZES || {};

DAR.QUIZZES["llm-engineering"] = {
  title: "LLM Engineering — Concept Examination",
  course: "AI 310",
  perSitting: 12,
  questions: [
    // ---- The papers canon ----
    { q: "In \"Attention Is All You Need\", scores are computed as $\\frac{QK^T}{\\sqrt{d_k}}$. Why divide by $\\sqrt{d_k}$?",
      opts: ["To normalize the output to unit length", "Dot products grow with dimension, pushing softmax into saturated regions with tiny gradients", "To make attention causal", "To reduce memory usage"], a: 1,
      expl: "For random $d_k$-dimensional vectors the dot product has variance $\\propto d_k$; dividing by $\\sqrt{d_k}$ keeps logits in softmax's sensitive range." },
    { q: "ResNet's core contribution was:",
      opts: ["Deeper networks via more filters", "Skip connections that let layers learn residuals, keeping very deep networks trainable", "Batch normalization", "The ReLU activation"], a: 1,
      expl: "Identity shortcuts give gradients a direct highway and reframe each block as learning a correction — the fix for the degradation problem in very deep nets." },
    { q: "Chinchilla's headline finding:",
      opts: ["Bigger models are always better", "Compute-optimal training uses roughly 20 tokens per parameter — most LLMs of the time were undertrained on data", "Data quality doesn't matter", "Scaling laws break beyond 100B parameters"], a: 1,
      expl: "For a fixed compute budget, model size and training tokens should scale together (~20:1 tokens:params). Gopher-sized models were data-starved; Chinchilla beat them at a quarter of the size." },
    { q: "LoRA finetunes a frozen weight matrix $W$ by learning:",
      opts: ["A sparse mask over $W$", "A low-rank update $\\Delta W = BA$ with rank $r \\ll d$, training only $A$ and $B$", "A new final layer only", "Quantized copies of $W$"], a: 1,
      expl: "Two thin matrices carry the task-specific update — under 1% of parameters, tiny optimizer state, and adapters you can hot-swap over one base model." },
    { q: "DPO's improvement over the RLHF pipeline is:",
      opts: ["A larger reward model", "Deriving a closed-form loss on preference pairs, removing the separate reward model and the RL loop entirely", "Faster PPO rollouts", "Human labels are no longer needed"], a: 1,
      expl: "DPO's key move: the RLHF objective has an analytic solution, so preference data can train the policy directly with a simple classification-style loss." },
    { q: "ViT processes an image by:",
      opts: ["Sliding convolutions of increasing depth", "Splitting it into fixed-size patches, embedding each, and running a standard transformer over the patch sequence", "Processing raw pixels row by row with an RNN", "Fourier-transforming the image first"], a: 1,
      expl: "\"An image is worth 16×16 words\" — patches are tokens. With enough data, the transformer matches or beats CNN inductive biases." },
    { q: "CLIP is trained with:",
      opts: ["Supervised ImageNet labels", "A contrastive objective aligning matching image–text pairs and repelling mismatched ones across a batch", "Next-pixel prediction", "Reinforcement learning from human feedback"], a: 1,
      expl: "Two encoders, one shared embedding space — which is what makes zero-shot classification possible: classify by comparing to text prompts." },
    { q: "BERT vs GPT pretraining:",
      opts: ["Both use causal language modeling", "BERT uses bidirectional masked-token prediction; GPT predicts the next token left-to-right", "BERT is autoregressive; GPT is not", "They differ only in size"], a: 1,
      expl: "Masked LM sees both sides of context — great for understanding tasks; causal LM can generate. The generative path won the scaling race." },
    { q: "The InstructGPT pipeline, in order:",
      opts: ["RL → reward model → finetuning", "Supervised finetuning → train a reward model on human preference rankings → optimize the policy with PPO", "Pretraining → DPO → SFT", "Reward model → pretraining → RL"], a: 1,
      expl: "SFT teaches the format, the reward model captures human preference, PPO pushes the policy toward it (with a KL leash to stay close to SFT)." },
    { q: "DDPM trains a network to:",
      opts: ["Generate images in one forward pass", "Predict the noise added at each step of a gradual noising process, so sampling can reverse it", "Discriminate real from fake images", "Autoregress over pixels"], a: 1,
      expl: "Forward: destroy data with scheduled Gaussian noise. Learned reverse: denoise step by step. The simplified objective is just MSE on the predicted noise." },
    { q: "FlashAttention achieves exact attention with less memory by:",
      opts: ["Approximating softmax with a kernel trick", "Tiling Q/K/V through fast on-chip SRAM and computing softmax online, never materializing the $N \\times N$ score matrix", "Dropping attention heads", "Using int8 arithmetic"], a: 1,
      expl: "It's an IO-aware reordering of the same math — memory drops from $O(N^2)$ to $O(N)$ and it's FASTER because HBM traffic, not FLOPs, was the bottleneck." },
    { q: "In a Mixtral-style MoE layer, \"active\" vs \"total\" parameters means:",
      opts: ["Nothing — all parameters are always used", "A router sends each token to $k$ of $N$ expert MLPs, so per-token compute uses only the chosen experts while ALL experts must sit in memory", "Only the router is trained", "Experts share all their weights"], a: 1,
      expl: "Mixtral 8x7B: ~47B total, ~13B active per token (top-2 of 8). Compute scales with active; memory scales with total — the MoE tradeoff in one line." },

    // ---- Systems math ----
    { q: "A 7B-parameter model stored in bf16 (2 bytes/param) needs approximately how many GB for weights alone?",
      num: 14, tol: 0.5,
      expl: "$7\\text{B} \\times 2$ bytes $= 14$ GB. The first number to compute before renting any GPU." },
    { q: "GPT-2 small (124M parameters) in fp32 (4 bytes/param) needs approximately how many MB for weights?",
      num: 496, tol: 10,
      expl: "$124\\text{M} \\times 4 = 496$ MB. Weights alone — training multiplies this several times over." },
    { q: "Full finetuning with Adam in mixed precision needs roughly how much memory relative to bf16 weights alone?",
      opts: ["The same", "About 2×", "Roughly 6–8× (weights + gradients + fp32 master weights and two Adam moments)", "Over 100×"], a: 2,
      expl: "Per parameter: bf16 weight (2B) + bf16 grad (2B) + fp32 master (4B) + two fp32 Adam states (8B) ≈ 16 bytes vs 2 — why a 7B full finetune laughs at a 24GB card, and why LoRA/ZeRO exist." },
    { q: "KV-cache size per token $\\approx 2 \\times n_{layers} \\times d_{model} \\times$ bytes. For 32 layers, $d_{model} = 4096$, bf16 (2 bytes): how many MB per token?",
      num: 0.5, tol: 0.05,
      expl: "$2 \\cdot 32 \\cdot 4096 \\cdot 2 = 524{,}288$ bytes $= 0.5$ MB per token — a 4k-token context holds ~2 GB of cache. This is what PagedAttention and GQA fight." },
    { q: "Self-attention compute scales with sequence length $N$ as:",
      opts: ["$O(N)$", "$O(N \\log N)$", "$O(N^2)$", "$O(2^N)$"], a: 2,
      expl: "Every token attends to every other. Long-context work is a war against this quadratic." },
    { q: "Average cross-entropy loss of 2.079 nats per token corresponds to a perplexity of approximately:",
      num: 8, tol: 0.2,
      expl: "Perplexity $= e^{2.079} \\approx 8$ — the model is 'as confused as' a uniform choice among 8 tokens. Report the loss's exponential, always." },
    { q: "Gradient checkpointing trades:",
      opts: ["Accuracy for speed", "Extra recomputation in the backward pass (~1 extra forward) for large activation-memory savings", "Model quality for memory", "Determinism for speed"], a: 1,
      expl: "Store only checkpoint activations, recompute the rest during backward — roughly 30% more compute for dramatically less memory." },
    { q: "Data parallelism vs tensor parallelism:",
      opts: ["They are the same thing", "Data parallel replicates the model and splits the batch; tensor parallel splits individual weight matrices across devices", "Tensor parallel only works at inference", "Data parallel needs no communication"], a: 1,
      expl: "DP syncs gradients once per step (all-reduce); TP communicates inside every layer — which is why TP wants NVLink-class bandwidth and DP tolerates slower links." },
    { q: "RoPE (rotary position embeddings) encodes position by:",
      opts: ["Adding a learned vector per position", "Rotating query/key vectors by position-dependent angles so their dot product depends on RELATIVE position", "Concatenating the position index", "A separate position network"], a: 1,
      expl: "Rotation makes $q_m \\cdot k_n$ a function of $m - n$ — relative offsets fall out of the geometry. Used by LLaMA and nearly every modern open model." },

    // ---- Engineering practice ----
    { q: "BPE tokenization builds its vocabulary by:",
      opts: ["Splitting on whitespace", "Iteratively merging the most frequent adjacent symbol pairs until the vocab budget is reached", "Using a fixed dictionary of English words", "One token per character, always"], a: 1,
      expl: "Frequent strings become single tokens, rare ones decompose into pieces — no out-of-vocabulary tokens, adjustable granularity. You'll build this from scratch in the tokenizer lab." },
    { q: "Sampling temperature $T$ works by:",
      opts: ["Truncating the vocabulary", "Dividing logits by $T$ before softmax — $T \\to 0$ approaches greedy decoding, $T > 1$ flattens the distribution", "Adding Gaussian noise to embeddings", "Scaling the KV cache"], a: 1,
      expl: "Temperature reshapes confidence: low sharpens (deterministic, repetitive), high flattens (creative, error-prone)." },
    { q: "Top-p (nucleus) sampling:",
      opts: ["Always keeps exactly $p$ tokens", "Samples from the smallest token set whose cumulative probability exceeds $p$", "Picks the single most likely token with probability $p$", "Is identical to temperature scaling"], a: 1,
      expl: "The candidate set adapts to the model's confidence — narrow when it's sure, wide when it's not. Usually combined with temperature." },
    { q: "Benchmark contamination means:",
      opts: ["Benchmarks with labeling errors", "Test data leaked into the pretraining corpus, inflating scores without real capability", "Too few benchmark samples", "Adversarial benchmark prompts"], a: 1,
      expl: "The web-scale corpus likely contains the benchmark itself. Treat public-benchmark claims with suspicion; build private evals for anything you actually ship." },
    { q: "Speculative decoding speeds up inference by:",
      opts: ["Skipping layers for easy tokens", "A small draft model proposing several tokens that the large model verifies in one parallel pass — output distribution provably unchanged", "Caching common completions", "Reducing precision to int4"], a: 1,
      expl: "Verification accepts a run of draft tokens per big-model pass. Exactness is the beautiful part: rejection sampling preserves the target distribution." },
    { q: "Weight-only quantization to int4:",
      opts: ["Always halves model quality", "Cuts weight memory ~4× vs bf16 with modest quality loss — activations and KV cache are the harder parts to quantize", "Speeds up training", "Only works on CPUs"], a: 1,
      expl: "Weights tolerate low precision well (GPTQ/AWQ). The engineering fight moves to activation outliers and cache precision." },
    { q: "vLLM's PagedAttention improves serving throughput by:",
      opts: ["Approximating attention", "Managing the KV cache in fixed-size pages (like virtual memory), eliminating fragmentation so far more concurrent sequences fit", "Skipping the KV cache", "Compressing prompts"], a: 1,
      expl: "Contiguous per-request cache allocation wastes memory on unpredictable lengths; paging packs it tight and shares prefixes." },
    { q: "Why does LLM pretraining use learning-rate warmup?",
      opts: ["It's cargo-culted with no benefit", "Adam's moment estimates are unreliable in the first steps and large early updates can destabilize training irreversibly", "It increases the final learning rate", "It only matters for small models"], a: 1,
      expl: "A few thousand gentle steps let optimizer statistics settle before full-size updates — cheap insurance against a run that never recovers." },
    { q: "Neural scaling laws describe test loss as:",
      opts: ["Constant beyond 1B parameters", "A power law in model size, data, and compute — smooth, predictable improvement across orders of magnitude", "Linear in parameter count", "Random across scales"], a: 1,
      expl: "Straight lines on log-log plots let labs forecast the value of compute before spending it — the discovery that turned scale into a strategy (and Chinchilla refined the recipe)." },
  ],
};
