// Brickford — MATH 210 question bank (30). Each sitting draws 12 at random.
// Coverage: matrix calculus (12), optimization (10), information theory (8).
window.DAR = window.DAR || {};
DAR.QUIZZES = DAR.QUIZZES || {};

DAR.QUIZZES["math-for-ml"] = {
  title: "Mathematics for ML — Concept Examination",
  course: "MATH 210",
  perSitting: 12,
  questions: [
    // ---- Matrix calculus ----
    { q: "For $f: \\mathbb{R}^n \\to \\mathbb{R}$, what is the shape of the gradient $\\nabla f$?",
      opts: ["A scalar", "An $n$-vector", "An $n \\times n$ matrix", "It depends on $f$"], a: 1,
      expl: "A scalar-valued function of $n$ inputs has one partial derivative per input — an $n$-vector. This shape discipline is the core skill of matrix calculus." },
    { q: "For $f: \\mathbb{R}^n \\to \\mathbb{R}^m$, the Jacobian $J$ has shape:",
      opts: ["$n \\times m$", "$m \\times n$", "$m \\times m$", "$n \\times n$"], a: 1,
      expl: "One row per output, one column per input: $J_{ij} = \\partial f_i / \\partial x_j$, so $m \\times n$." },
    { q: "$\\dfrac{\\partial}{\\partial x}\\left(x^T A x\\right) = $",
      opts: ["$2Ax$ always", "$(A + A^T)x$", "$A^T x$", "$x^T A$"], a: 1,
      expl: "In general $(A+A^T)x$; it collapses to $2Ax$ only when $A$ is symmetric. Deep learning losses usually involve symmetric forms, which is why you often see $2Ax$." },
    { q: "For the linear map $f(x) = Ax$, the Jacobian is:",
      opts: ["$A^T$", "$A$", "$x A$", "$\\|A\\|$"], a: 1,
      expl: "A linear map is its own best linear approximation — the Jacobian of $Ax$ is exactly $A$." },
    { q: "Chain rule with shapes: $g:\\mathbb{R}^n\\to\\mathbb{R}^m$, $f:\\mathbb{R}^m\\to\\mathbb{R}$, $h = f \\circ g$. Then $\\nabla h(x) =$",
      opts: ["$J_g \\, \\nabla f$", "$J_g^T \\, \\nabla f(g(x))$", "$\\nabla f(g(x))^T J_g^T$", "$\\nabla f \\cdot \\nabla g$"], a: 1,
      expl: "$J_g^T$ is $n \\times m$ and $\\nabla f$ is $m$-dimensional, giving an $n$-vector. Backpropagation is exactly this: transpose-Jacobian times upstream gradient." },
    { q: "$\\dfrac{\\partial}{\\partial A}\\,\\mathrm{tr}(AB) = $",
      opts: ["$B$", "$B^T$", "$A^T$", "$\\mathrm{tr}(B)\\,I$"], a: 1,
      expl: "$\\mathrm{tr}(AB) = \\sum_{ij} A_{ij}B_{ji}$, so $\\partial/\\partial A_{ij} = B_{ji}$ — i.e. $B^T$. Trace identities are the workhorse of matrix gradient derivations." },
    { q: "Jacobi's formula: for invertible $A$, $\\dfrac{\\partial \\det A}{\\partial A} = $",
      opts: ["$\\det(A) \\, A^{-T}$", "$A^{-1}$", "$\\det(A) \\, A$", "$\\mathrm{adj}(A)^T$"], a: 0,
      expl: "$d(\\det A) = \\det(A)\\,\\mathrm{tr}(A^{-1} dA)$, which as a gradient reads $\\det(A) A^{-T}$. This appears in normalizing flows and log-det terms." },
    { q: "$f(x, y) = x^2 y + 3y$. Compute $\\partial f / \\partial y$ at $(x,y) = (2, 1)$.",
      num: 7, tol: 0.001,
      expl: "$\\partial f/\\partial y = x^2 + 3 = 4 + 3 = 7$. Partials treat the other variables as constants." },
    { q: "The Hessian of a twice continuously differentiable $f:\\mathbb{R}^n\\to\\mathbb{R}$ is:",
      opts: ["Always positive definite", "Always symmetric", "Always diagonal", "The same as the Jacobian"], a: 1,
      expl: "Schwarz's theorem: mixed partials commute, so $H_{ij} = H_{ji}$. Positive definiteness only holds at strict local minima." },
    { q: "$\\dfrac{\\partial}{\\partial x} \\|x\\|_2^2 = $",
      opts: ["$x$", "$2x$", "$2\\|x\\|$", "$x/\\|x\\|$"], a: 1,
      expl: "$\\|x\\|^2 = x^T x$, and the quadratic-form rule with $A = I$ gives $2x$. The single most-used gradient in ML." },
    { q: "Reverse-mode automatic differentiation (backprop) is preferred in deep learning because:",
      opts: ["It is numerically more accurate than forward mode", "One backward pass gives the gradient w.r.t. ALL inputs when the output is a scalar loss", "It uses less memory than forward mode", "It avoids the chain rule"], a: 1,
      expl: "Reverse mode costs one pass per OUTPUT; training has one scalar loss and millions of parameters, so one backward pass beats millions of forward-mode passes. The price is storing activations — hence more memory, not less." },
    { q: "Forward-mode autodiff via dual numbers computes, in one pass:",
      opts: ["The full Jacobian", "One directional derivative (Jacobian-vector product)", "The Hessian", "The gradient of every parameter"], a: 1,
      expl: "Each forward pass propagates one tangent vector — a JVP. Full Jacobians need one pass per input dimension, which is why forward mode suits few-input problems." },

    // ---- Optimization ----
    { q: "The gradient descent update with learning rate $\\eta$ is:",
      opts: ["$x \\leftarrow x + \\eta \\nabla f(x)$", "$x \\leftarrow x - \\eta \\nabla f(x)$", "$x \\leftarrow -\\eta \\, x$", "$x \\leftarrow x - \\nabla f(\\eta x)$"], a: 1,
      expl: "Step against the gradient — the direction of steepest local decrease." },
    { q: "$f(x) = x^2$, starting at $x_0 = 3$ with $\\eta = 0.1$. After one gradient step, $x_1 = $",
      num: 2.4, tol: 0.001,
      expl: "$f'(3) = 6$, so $x_1 = 3 - 0.1 \\cdot 6 = 2.4$." },
    { q: "For a convex function, every local minimum is:",
      opts: ["A saddle point", "A global minimum", "Unique", "At the origin"], a: 1,
      expl: "Convexity's superpower: the bowl has no false valleys. (The minimum need not be unique — think of a flat-bottomed bowl.)" },
    { q: "Momentum helps gradient descent primarily by:",
      opts: ["Increasing the learning rate automatically", "Accumulating a running average of gradients, damping oscillation across steep directions while accelerating along consistent ones", "Guaranteeing convergence to a global minimum", "Making the loss convex"], a: 1,
      expl: "In a ravine, plain GD zig-zags across the walls; momentum's exponential average cancels the zig-zag components and builds speed along the valley floor." },
    { q: "Adam differs from SGD-with-momentum by additionally:",
      opts: ["Using second-order Hessian information", "Scaling each parameter's step by a running estimate of the gradient's second moment (per-parameter adaptive step sizes)", "Removing the need for a learning rate", "Computing exact line searches"], a: 1,
      expl: "Adam keeps both a first-moment (momentum) and second-moment estimate; dividing by $\\sqrt{\\hat v}$ gives each parameter its own effective step size." },
    { q: "Adam's bias correction ($\\hat m = m / (1 - \\beta_1^t)$) exists because:",
      opts: ["Gradients are biased estimators", "The moment estimates start at zero and are underestimated in early steps", "It prevents overfitting", "$\\beta_1$ decays over time"], a: 1,
      expl: "With $m_0 = 0$, the exponential average needs several steps to warm up; dividing by $1-\\beta^t$ exactly cancels that startup bias." },
    { q: "A learning rate that is clearly too high typically produces:",
      opts: ["Slow, smooth convergence", "A loss that oscillates or diverges", "Guaranteed convergence to a saddle", "No effect if using Adam"], a: 1,
      expl: "Steps overshoot the minimum and bounce or explode. The classic first diagnostic when loss goes to NaN." },
    { q: "In high-dimensional deep learning loss landscapes, the dominant obstacle is believed to be:",
      opts: ["Bad local minima everywhere", "Saddle points and plateaus rather than bad local minima", "Discontinuities", "Non-differentiability"], a: 1,
      expl: "In high dimensions a critical point needs every direction to curve upward to be a true minimum — overwhelmingly unlikely; most critical points are saddles." },
    { q: "Weight decay corresponds to:",
      opts: ["Dropout on the weights", "Shrinking weights toward zero each step (an $L_2$-style penalty; decoupled from the gradient in AdamW)", "Decaying the learning rate", "Clipping large gradients"], a: 1,
      expl: "Each update multiplies weights by $(1 - \\eta\\lambda)$. AdamW's insight: apply it directly rather than through Adam's adaptive scaling." },
    { q: "Learning-rate warmup followed by cosine decay is used because:",
      opts: ["It maximizes the final learning rate", "Early optimizer statistics are noisy — a small initial rate avoids instability, then a smooth decay lets training settle into a sharp minimum", "It is required for convergence proofs", "It prevents all overfitting"], a: 1,
      expl: "Warmup protects the fragile early phase (especially with Adam's untrustworthy early second moments); cosine decay anneals steps so the model can settle." },

    // ---- Information theory ----
    { q: "The entropy of a fair coin flip, in bits:",
      num: 1, tol: 0.001,
      expl: "$H = -\\left(\\tfrac12\\log_2\\tfrac12 + \\tfrac12\\log_2\\tfrac12\\right) = 1$ bit — the unit itself is defined by this case." },
    { q: "The entropy of a uniform distribution over 8 outcomes, in bits:",
      num: 3, tol: 0.001,
      expl: "$H = \\log_2 8 = 3$. Uniform distributions maximize entropy — total uncertainty, three yes/no questions to pin down." },
    { q: "Shannon entropy is defined as:",
      opts: ["$\\sum_x p(x) \\log p(x)$", "$-\\sum_x p(x) \\log p(x)$", "$\\sum_x \\log p(x)$", "$-\\max_x \\log p(x)$"], a: 1,
      expl: "The expected surprise $-\\log p(x)$. Rare events carry more information; entropy is the average." },
    { q: "Which is TRUE of KL divergence $D_{KL}(p \\| q)$?",
      opts: ["It is symmetric in $p$ and $q$", "It is $\\geq 0$, and $= 0$ iff $p = q$", "It is a metric", "It can be negative for continuous distributions"], a: 1,
      expl: "Gibbs' inequality gives non-negativity with equality iff the distributions match. It is NOT symmetric — $D_{KL}(p\\|q) \\neq D_{KL}(q\\|p)$ — which is why it isn't a metric." },
    { q: "Cross-entropy decomposes as $H(p, q) = $",
      opts: ["$H(p) - D_{KL}(p \\| q)$", "$H(p) + D_{KL}(p \\| q)$", "$H(q) + D_{KL}(q \\| p)$", "$D_{KL}(p \\| q) - H(p)$"], a: 1,
      expl: "Since $H(p)$ is fixed by the data, minimizing cross-entropy in $q$ is exactly minimizing $D_{KL}(p\\|q)$ — this is why cross-entropy is THE loss for classification and language modeling." },
    { q: "Training a language model by minimizing negative log-likelihood is equivalent to:",
      opts: ["Maximizing entropy", "Minimizing the cross-entropy between the empirical data distribution and the model", "Minimizing the model's perplexity on the training set — both this and the cross-entropy statement", "Minimizing KL from the model to a uniform prior"], a: 2,
      expl: "NLL = cross-entropy against the empirical distribution, and perplexity is just $e^{\\text{NLL}}$ — three names for one objective." },
    { q: "Mutual information $I(X;Y) = H(X) - H(X|Y)$ equals zero exactly when:",
      opts: ["$X$ and $Y$ are identical", "$X$ and $Y$ are independent", "$H(X) = H(Y)$", "$Y$ is deterministic"], a: 1,
      expl: "Knowing $Y$ reduces uncertainty about $X$ by $I(X;Y)$ bits; independence means knowing $Y$ tells you nothing." },
    { q: "Shannon's source coding theorem says:",
      opts: ["Any data can be compressed to arbitrary size", "Lossless compression cannot beat the entropy of the source (in bits per symbol, on average)", "Compression requires a noisy channel", "Entropy limits only apply to text"], a: 1,
      expl: "Entropy is the hard floor for lossless compression. Deep connection to LLMs: a better language model IS a better compressor of text." },
  ],
};
